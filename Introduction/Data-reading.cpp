#include <stdio.h>

int main(){
    float numero; //Declaramos una variable
    printf("Digite un numero: "); //Pedimos que se ingrese un numero
    scanf("%f",&numero); //Guardamos el numero en la variable 
    printf("\n El numero que digito es: %f",numero); //Imprimimos el numero guardado en la variable 
    return 0; //Esperamos a que el program anos retorne cero
}